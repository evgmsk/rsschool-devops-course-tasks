# External secrets - create these manually before deploying Jenkins
# kubectl create secret generic jenkins-aws-credentials --from-literal=aws-access-key-id=YOUR_KEY --from-literal=aws-secret-access-key=YOUR_SECRET
# kubectl create secret generic jenkins-sonar-token --from-literal=token=YOUR_SONAR_TOKEN